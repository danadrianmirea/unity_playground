using UnityEngine;
//using TMPro; // If using TextMeshPro

public class InstanceCountDisplay : MonoBehaviour
{
    //public GridSpawner spawner;        // Reference to your spawner
    //public TextMeshProUGUI countText; // Assign via inspector

    void Update()
    {
        /*
        if (spawner != null && countText != null)
        {
            countText.text = $"Instances spawned: {spawner.totalSpawned}";
        }
        */
    }
}
